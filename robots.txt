# DA Infotech
#
# The origin below must match the canonical link in index.html and the <loc>
# in sitemap.xml. If a custom domain replaces the Vercel one, change all three
# in the same commit — a sitemap on the wrong origin is silently discarded.

User-agent: *
Allow: /

# The contact endpoint is not a page. Nothing to index, and nothing there
# answers a GET.
Disallow: /api/

Sitemap: https://da-infotech-website.vercel.app/sitemap.xml
